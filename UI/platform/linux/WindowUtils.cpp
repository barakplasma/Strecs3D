#include "../WindowUtils.h"

namespace Platform {
namespace WindowUtils {

void customizeTitleBar(QWidget* window) {
    // Currently no specific customization for Linux
    (void)window;
}

} // namespace WindowUtils
} // namespace Platform
